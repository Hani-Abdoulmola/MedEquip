## دليل مختصر للأدوار والصلاحيات (Authorization Guide)

### 1. المبادئ العامة

- **الصلاحيات (Permissions)** هي المصدر الحقيقي للقرار في الكود:  
  - استخدم دائمًا `can('permission-name')` أو `$user->can('permission-name')` في السياسات والواجهات.
- **الأدوار (Roles)** هي قوالب جاهزة لجمع صلاحيات، وليست بديلاً عن التحقق من الصلاحيات.
- أدوار الهوية مثل `Supplier` و`Buyer` تُستخدم في منطق المورد/المشتري (Policies, Middleware)، وليست أدوار موظفين.

### 2. استخدام Spatie في الموديلات

- نموذج المستخدم `App\Models\User`:
  - يستخدم Trait: `Spatie\Permission\Traits\HasRoles`.
- النماذج المخصصة:
  - `App\Models\Permission` و`App\Models\Role` ترث من نماذج Spatie وتضيف حقل `ar_name` لعرض الأسماء بالعربية.
- إعدادات الحزمة:
  - معرفة الموديلات تتم من خلال `config/permission.php`، حيث تم ضبطها لاستخدام الموديلات المخصصة.

### 3. السياسات (Policies)

- يتم ربط السياسات في `AuthServiceProvider` على النماذج المخصصة:
  - `Role::class => RolePolicy::class`
  - `Permission::class => PermissionPolicy::class`
- داخل السياسات يفضّل:
  - الاعتماد على الصلاحيات:
    ```php
    return $user->can('roles.view');
    ```
  - مع إمكانية إعطاء دور `Admin` صلاحيات أوسع إذا لزم الأمر.

### 4. إنشاء مستخدمين داخليين

- الأدمن ينشئ مستخدمين داخليين فقط (نوع المستخدم = مدير النظام أو أنواع داخلية أخرى).
- يمكنه:
  - تعيين دور داخلي للمستخدم (`Admin`, `Staff`, أدوار مخصصة).
  - تعيين صلاحيات تفصيلية مباشرة للمستخدم من شاشة "إدارة الصلاحيات".
- القرار النهائي في الكود يعتمد على:
  - الصلاحيات (`can()`)، وليس فقط على الدور الذي يحمله المستخدم.

### 5. الأسماء العربية للصلاحيات والأدوار

- الحقول:
  - `permissions.name` = اسم النظام (إنجليزي، ثابت).
  - `permissions.ar_name` = التسمية العربية التي تظهر في واجهة الإدارة.
  - `roles.name` / `roles.ar_name` بنفس الفكرة.
- في Blade:
  - لعرض اسم الصلاحية:
    ```blade
    {{ $permission->ar_name ?? $permission->name }}
    ```
  - لعرض اسم الدور:
    ```blade
    {{ $role->ar_name ?? $role->name }}
    ```

### 6. أفضل ممارسات عند إضافة كيان جديد

1. إضافة صلاحيات جديدة في Seeder الصلاحيات (مثلاً `invoices.approve`).
2. إضافة أو تعديل الأدوار الافتراضية لإسناد هذه الصلاحيات (عند الحاجة).
3. تحديث السياسات (Policies) للكيان الجديد لتستخدم الصلاحيات المناسبة.
4. تحديث واجهة الإدارة لإظهار الصلاحيات الجديدة بالعربية ضمن المجموعة الصحيحة (module).


